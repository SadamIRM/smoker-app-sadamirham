import 'package:flutter/material.dart';

class AppColors {
  // Smoky Slate, Ash & Ember Theme
  static const Color primary = Color(0xFF37474F);      // Dark Smoke Slate (Charcoal)
  static const Color primaryLight = Color(0xFF78909C); // Medium Smoke / Ash
  static const Color primaryDark = Color(0xFF263238);  // Deep Charcoal
  static const Color accent = Color(0xFFFF5722);       // Ember Orange (Fire tip)

  static const Color background = Color(0xFFECEFF1);   // Light Ash Background
  static const Color surface = Color(0xFFFFFFFF);      // White
  static const Color error = Color(0xFFD32F2F);        // Red
  static const Color info = Color(0xFF0288D1);         // Blue

  static const Color textPrimary = Color(0xFF212121);  // Charcoal Text
  static const Color textSecondary = Color(0xFF546E7A);// Muted Blue-Grey Text
  static const Color textHint = Color(0xFFB0BEC5);     // Silver Hint
  static const Color textOnPrimary = Color(0xFFFFFFFF);// White text on primary

  static const Color divider = Color(0xFFCFD8DC);      // Soft Ash Divider
  static const Color border = Color(0xFFB0BEC5);       // Soft Ash Border
}